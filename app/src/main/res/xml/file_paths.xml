<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Répertoire des fichiers externes de l'application -->
    <external-files-path
        name="external_files"
        path="." />
    
    <!-- Répertoire de cache externe -->
    <external-cache-path
        name="external_cache"
        path="." />
    
    <!-- Répertoire des fichiers internes -->
    <files-path
        name="internal_files"
        path="." />
    
    <!-- Répertoire de cache interne -->
    <cache-path
        name="internal_cache"
        path="." />
    
    <!-- Stockage externe pour les exports CSV et PDF -->
    <external-path
        name="external_storage"
        path="." />
</paths>
